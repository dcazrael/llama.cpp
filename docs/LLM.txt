Qwen3.8 Maya Optimization Agent Reference

READ THIS FIRST
---------------
This branch is part of a two-repo implementation/benchmark workflow with
dcazrael/llm-workbench.

The assistant/agent owns implementation work. The user runs the prepared
benchmark command on Maya and pushes results.

WORKING LOOP
------------
When benchmark evidence identifies a concrete bottleneck and a plausible fix:
1. Inspect the latest pushed workbench logs/results.
2. Implement the fix on this branch.
3. Preserve existing Generel functionality unless the change explicitly
   replaces it.
4. Update architectural/bugfix documentation in this repo.
5. Update the canonical workbench suite if the tested configuration changes.
6. Commit both repos as needed.
7. Tell the user when to pull and give one command to run.
8. After the user pushes results, inspect them and implement the next concrete
   fix if required.

Do not send the user into manual implementation, branch juggling, ad-hoc build
commands, or broad test matrices when the root cause is already established.
Do not create disposable scripts for every hypothesis.

CURRENT BRANCH
--------------
Branch:
  maya/qwen38-generel-multigpu

Build used by workbench:
  /home/maya/Applications/llama.cpp-dual-gpu-opt/build-multigpu/bin/llama-server

Base intent:
- Generel multi-GPU MoE-cache stack.
- Qwen3.8-Flash-Next / Qwen4Exp optimizations.
- Preserve high outer ubatch at deep context.
- Direct target: ~128K active prompt at ubatch 8192 on Maya's dual RTX 3060s.

CURRENT QSA DESIGN
------------------
The chunked QSA graph copies its host block-bias input once before taking
query-chunk views. Taking views of the host input first made the scheduler
keep one CUDA copy per chunk and per QSA layer. At 32768 KV cells, a diagnostic
reservation had 252 live 6 MiB bias copies on CUDA1. The first prefill
reservation fell from 4629.81 MiB to 4309.81 MiB after the shared copy;
at the step after 49152 tokens it fell from 7081.69 MiB to 4841.69 MiB.
The diagnostic 2026-09-23T103859Z target completed 130000 prompt tokens and
256 generated tokens at 327.97 PP tok/s and 7.16 TG tok/s. CUDA1's last
prefill workspace was 5792.32 MiB. The temporary allocator tracing used to
identify the repeated copies was removed from source after this run.
The final clean 2026-09-23T105047Z run also passed at 326.81 PP tok/s and
7.10 TG tok/s with the same 130000 prompt and 256 generated tokens.

The old indexer-score-only chunking experiment proved insufficient. Shrinking
that scratch target did not materially change the fatal deep-context workspace.

The active implementation now uses attention-side QSA prefill chunking plus
block-granular selection:
- outer llama ubatch remains unchanged,
- QSA internally uses adaptive 64..256-token query chunks,
- avoids materializing the full [n_kv, ubatch] QSA attention mask for the
  normal single-stream causal path,
- rebuilds causal visibility from cache cell positions,
- ranks compressed blocks before expanding only the selected blocks to cache
  cells; the incomplete causal tail is carried separately,
- avoids the n_kv-wide F32 score expansion and n_kv-wide top-k sort on the
  normal block-bias path,
- still materializes one [n_kv, n_chunk] F16 sparse-attention mask; removing
  that mask/its mask-to-indices rescan is a separate remaining optimization,
- routes both chunked and normal QSA into sparse flash attention when the
  Qwen4Exp MMA sparse shape is supported,
- includes the later future-cell causal-selection correction,
- preserves Generel sampled-decode support.

Relevant design/history:
  docs/decisions/2026-09-17-qsa-indexer-chunking.md

QSA OPTIMIZATION INVARIANTS — DO NOT REGRESS
-----------------------------------------
The active 128K/ub8192 work keeps these three implementation angles together:

1. Native q4_0 K/V in MMA Flash Attention. q4 KV is dequantized per MMA tile;
   do not restore full-cache F16 staging.
2. Block-granular QSA top-k in both normal and chunked paths. Rank compressed
   blocks first, expand only selected blocks, and carry the incomplete causal
   tail separately; do not restore n_kv-wide F32 score expansion/sort on the
   normal causal path.
3. Sparse Flash Attention on the normal QSA path as well as the chunked QSA
   path when the Qwen4Exp MMA sparse shape is supported; do not restore the
   dense KV scan.

The next deeper memory optimization remains removal of the residual
[n_kv, n_chunk] F16 sparse-attention mask / mask-to-indices rescan. That is a
kernel/API change and must preserve per-query causal membership inside a shared
query tile.

LATEST 128K / UB8192 MEMORY EVIDENCE
-----------------------------------
The ISTA GSQ-RCO IQ3_XXS cache48 run on 2026-09-22 processed two 8192-token
prompt chunks at about 484 and 462 tok/s, then failed before the third chunk
while reserving a 5907.82 MiB shared CUDA1 compute workspace.

Important observations:
- the failure is a scheduler workspace reserve, not a kernel allocation,
- the live-KV growth policy was rounding the required ~24576 cells to 32768,
- CUDA1 is the display GPU and was the first device to run out of headroom,
- prefill MoE cache telemetry showed 7675 hits / 13277 misses (36.63% hit rate)
  with 7328 MiB of demand H2D traffic before failure,
- the backend candidate replacement path retires grouped/legacy resources and
  frees their CUDA slot pools when n_slots changes.

Current mitigation:
- live-context KV workspace growth uses a 1024-cell alignment instead of
  power-of-two growth, so 24576 no longer reserves a 32768-cell graph,
- with phase-aware workspace enabled, runtime MoE candidate snapshots are
  capped to 16 slots during prefill,
- the model-configured cache size remains unchanged (48 in the canonical
  suite), and the runtime cap returns to configured size for decode,
- changing phase marks the candidate snapshot and scheduler reservation dirty,
  so backend resources are rebuilt only on the phase transition,
- before a growing live-KV workspace is reallocated, idle CUDA transient-pool
  pages are trimmed after synchronization so the replacement cudaMalloc does
  not compete with retained scratch mappings.

This is deliberately a residency/workspace change. It does not reduce outer
ubatch and must not regress the three QSA optimization invariants above.

BOUNDED MOE PREFILL EXECUTION
-----------------------------
Compact staged FP4 MMQ must supply a host route containing its remapped IDs,
with a null expert_map because staged weights already use compact expert IDs.
It previously passed host_route=nullptr and bypassed the 2048-token tiling
guard. Keep the remapped ID vector alive until all tiles have been dispatched.
The tile diagnostic includes src1/ids/dst dimensions and mapped-source status.
The existing FP4 overflow regression uses 2051 tokens to cover a full tile and
a partial tail against uncached CUDA output.
Local validation: the build and test-moe-cache --cached-fusion-only passed.
The canonical 2026-09-22T210839Z run still OOMed after 57344 tokens; its
backtrace includes the recursive tiled MMID call. The fix alone is insufficient.
Backend INFO messages map to TRACE and are hidden at verbosity 3. The workbench
suite now uses verbosity 4; absence of the old INFO line did not prove bypass.

A later target attempt progressed past the scheduler-allocation failure and
surfaced a CUDA VMM transient-pool allocation failure at cuMemCreate(). Treat
cuMemCreate/cudaMalloc as the point where physical VRAM exhaustion becomes
visible, not as the optimization target itself.

The cached MoE prefill path has token-proportional CUDA scratch:
- MMQ activation quantization buffers scale with the MMID token count.
- The generic MMID fallback allocates sorted input/output workspaces
  proportional to token count.
- Expert staging is a separate selected-expert cost and can be reused while
  token slices execute.

Current rule:
- preserve the llama-level outer ubatch (8192 on the target),
- for host-routed cached-expert MUL_MAT_ID with more than 2048 tokens, execute
  the CUDA MMID operation in 2048-token views,
- reuse the same staged expert set and route map across all token tiles,
- write each tile directly into its corresponding destination view,
- leave ordinary/non-cached MMID dispatch unchanged,
- emit one "moe-cache: tiling host-routed prefill MMID ..." line when the
  bounded path first activates.

This bounds token-proportional MoE scratch without sacrificing the 8192-token
attention/GDN graph geometry. If a later OOM still occurs, distinguish a
selected-expert staging peak from the remaining QSA mask/indices allocation
before changing allocator policy.

PREVIOUS 57K OOM EVIDENCE
--------------------------------
Previous failed target result:
  llm-workbench/llm-lab/qwen3.8-flash-next/server-suites/
  2026-09-22T203722Z-qwen38-QSA-chunking-128K-ub8192-ceiling/

Binary/source:
- branch maya/qwen38-generel-multigpu
- checkout/binary SHA 6f2e5462721f6e9575d6597f3335b88fc35994e9
- checkout matches binary

Observed progress before OOM:
- 8192   tokens: ~489.06 PP tok/s
- 16384  tokens: ~464.86 PP tok/s
- 24576  tokens: ~437.89 PP tok/s
- 32768  tokens: ~416.52 PP tok/s
- 40960  tokens: ~413.67 PP tok/s
- 49152  tokens: ~409.62 PP tok/s
- 57344  tokens: ~405.11 PP tok/s
- then CUDA1 OOM at ~3:12 server time.

The fatal stack is now specific:
  ggml_cuda_pool_vmm::alloc
    -> ggml_cuda_mul_mat_q_impl
    -> ggml_cuda_mul_mat_id_impl
    -> ggml_cuda_mul_mat_id_staged
    -> ggml_cuda_mul_mat_id_cached

The allocator failure is:
  cuMemCreate(&handle, reserve_size, &prop, 0)

GPU telemetry peaked around:
- CUDA0 ~10.82 GiB
- CUDA1 ~11.61 GiB
CUDA1 remains the display GPU and fails first.

CRITICAL NEW FINDING:
The bounded cached-MoE MMID path added at 6f2e546 was expected to emit:
  moe-cache: tiling host-routed prefill MMID tokens=... tile=2048
The latest server.log contains NO such line.

Therefore the current problem is not "invent another allocator workaround".
The current problem is:
- the actual staged cached-MoE MMQ shape that OOMs is bypassing the new tiling
  predicate/path,
- the backtrace proves the fatal allocation is inside ggml_cuda_mul_mat_q_impl
  reached through staged cached MUL_MAT_ID,
- inspect the real tensor dimensions/host_route form at that call,
- make bounded internal MMID tiling apply to that real path (or implement an
  equivalent bounded scratch strategy inside mul_mat_q_impl),
- keep outer llama ubatch 8192,
- preserve native-q4 FA, block-first QSA and sparse FA.

Do not spend time tuning VMM growth or adding allocator retries before fixing
the fact that the intended bounded MMID path did not activate.

BLOCK-FIRST INPUT ALLOCATION GOTCHA
----------------------------------
After block-granular top-k was introduced, the normal block-bias path no longer
consumed the n_kv-wide cell_blk input. Creating it anyway left the tensor with
no backend buffer; set_input_qsa then called ggml_backend_buffer_is_host() on
that null buffer and failed at ggml-backend.cpp:205.

Current invariant:
- cell_blk exists only for the fallback token-level selection path.
- cell_pos exists only when the device-causal chunk path consumes it.
- set_input_qsa must tolerate both pointers being null.
- llama_memory_hybrid_idx_context owns the cache-width lookup via
  get_idx()->get_n_kv() and passes n_kv explicitly to the low-level memory
  object; do not call zero-argument get_n_kv() on llama_kv_cache itself.
- never create graph inputs "just in case": unconsumed inputs may have no
  backend buffer.


NATIVE Q4 FLASH-ATTENTION PATH
------------------------------
The MMA flash-attention path supports direct q4_0 K/V tile loading so q4_0 KV
does not require full-cache F16 staging.

Important plumbing invariant:
- fattn_kernel_t, tile/vec/MMA kernel signatures, and launch_fattn must all carry
  the native-KV mode flag consistently.
- the MMA kernel must propagate q4 mode plus original byte strides through
  flash_attn_ext_f16 -> process_tile -> iter -> load_tile.
- host-side allocation accounting and the MMA launcher must use the same
  need_f16_K / need_f16_V decision.

A partial port previously changed the loader and shared kernel typedef without
propagating those arguments through the MMA call chain. NVCC then failed with
"no instance of function template flash_attn_ext_f16 matches the required
type". It also left the launcher requesting F16 staging even though allocation
accounting had disabled it for native q4_0. Commit b89af7c completes both
halves. Do not reintroduce either mismatch when rebasing FA changes.

CURRENT VALIDATION
------------------
Canonical workbench target:
  suites/qwen38-qsa-chunking-128k-ub8192.yaml

Canonical user command from ~/llm-workbench:
  bash tools/run-qsa-suite.sh

The workbench helper updates this branch, rebuilds build-multigpu, and runs the
canonical target. Do not create another one-off wrapper for the next hypothesis.

If the target fails, inspect the new allocation owner and implement the next
specific memory fix. Do not return to low-ubatch fallback mapping unless the
user explicitly asks for a fallback configuration.

DOCUMENTATION DISCIPLINE
------------------------
Every meaningful refactor, new optimization, architecture decision, or
important bugfix/gotcha on this branch must update this file and/or the relevant
decision doc in the same work cycle.
